<div class="col-lg-12">
  <%= form_for(@plate_rating) do |f| %>  
  <%= link_to raw("<button type='button' class='btn btn-default'>Voltar</button>"), plate_ratings_path %>
  <div class="panel"></div>
  <section class="panel">
    <header class="panel-heading">
       Rating de Prato
    </header>
    <% if @plate_rating.errors.any? %>
      <div class="alert alert-danger">
        <% @plate_rating.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </div>
    <% end %>
    <div class="panel-body">
                  <div class="form-group">
            <%= f.label 'Cliente*' %><br>
            <%= f.select :client_id, @clients.collect {|client| [ client.name, client.id]}, {:prompt => 'Selecione o Cliente'}, {:class => 'form-control'} %>
          </div>
                  <div class="form-group">
            <%= f.label 'Prato*' %><br>
            <%= f.select :plate_id, @plates.collect {|type| [ type.title, type.id]}, {:prompt => 'Selecione o Prato'}, {:class => 'form-control'} %>
          </div>
                  <div class="form-group">
            <%= f.label 'Score*' %><br>
            <%= f.number_field :score, :class => 'form-control' %>
          </div>
            </div>
  </section>
  <div class="form-group">
      <%= f.submit 'Salvar', :class => 'btn btn-primary' %>
  </div>
  <% end -%>
</div>