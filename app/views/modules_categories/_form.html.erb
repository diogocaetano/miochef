<%= form_for(@modules_category) do |f| %>
  <% if @modules_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@modules_category.errors.count, "error") %> prohibited this modules_category from being saved:</h2>

      <ul>
      <% @modules_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :icon %><br>
    <%= f.text_field :icon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
