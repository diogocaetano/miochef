<div class="col-lg-12">
  <%= form_for(@request) do |f| %>  
  <%= link_to raw("<button type='button' class='btn btn-default'>Voltar</button>"), requests_path %>
  <div class="panel"></div>
  <section class="panel">
    <header class="panel-heading">
       Requests
    </header>
    <% if @request.errors.any? %>
      <div class="alert alert-danger">
        <% @request.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </div>
    <% end %>
    <div class="panel-body">
                  <div class="form-group">
            <%= f.label :references %><br>
            <%= f.text_field :references, :class => 'form-control' %>
          </div>
                  <div class="form-group">
            <%= f.label :references %><br>
            <%= f.text_field :references, :class => 'form-control' %>
          </div>
                  <div class="form-group">
            <%= f.label :payment_code %><br>
            <%= f.text_field :payment_code, :class => 'form-control' %>
          </div>
                  <div class="form-group">
            <%= f.label :request_date %><br>
            <%= f.datetime_select :request_date, :class => 'form-control' %>
          </div>
                  <div class="form-group">
            <%= f.label :delivery_date %><br>
            <%= f.datetime_select :delivery_date, :class => 'form-control' %>
          </div>
                  <div class="form-group">
            <%= f.label :delivery_window %><br>
            <%= f.text_field :delivery_window, :class => 'form-control' %>
          </div>
                  <div class="form-group">
            <%= f.label :references %><br>
            <%= f.text_field :references, :class => 'form-control' %>
          </div>
            </div>
  </section>
  <div class="form-group">
      <%= f.submit 'Salvar', :class => 'btn btn-primary' %>
  </div>
  <% end -%>
</div>