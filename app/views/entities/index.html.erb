<p id="notice"><%= notice %></p>

<h1>Listing Entities</h1>

<table>
  <thead>
    <tr>
      <th>Modules categories</th>
      <th>Name</th>
      <th>Slug</th>
      <th>Controller</th>
      <th>Is dev</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @entities.each do |entity| %>
      <tr>
        <td><%= entity.modules_categories %></td>
        <td><%= entity.name %></td>
        <td><%= entity.slug %></td>
        <td><%= entity.controller %></td>
        <td><%= entity.is_dev %></td>
        <td><%= link_to 'Show', entity %></td>
        <td><%= link_to 'Edit', edit_entity_path(entity) %></td>
        <td><%= link_to 'Destroy', entity, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Entity', new_entity_path %>
